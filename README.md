# WpfArraysApp

WPF (.NET 8) car inventory app built with MVVM.

## Requirements
- Windows 10/11
- .NET 8 SDK
- Visual Studio 2022 (optional)

## Run
Option 1: Visual Studio
1. Open `WpfArraysApp.sln`.
2. Build and run the `WpfArraysApp` project.

Option 2: CLI
```
dotnet build

dotnet run --project .\WpfArraysApp\WpfArraysApp.csproj
```

## Notes
- Project docs and screenshots live under `WpfArraysApp`.
- Known bugs are listed in `WpfArraysApp\docs\KNOWN_BUGS.md`.
