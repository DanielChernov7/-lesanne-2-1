<Window x:Class="WpfArraysApp.Views.CarEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{StaticResource DialogTitle}" Height="420" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="{StaticResource IdLabel}" />
        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Id}" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="{StaticResource BrandLabel}" />
        <TextBox Grid.Row="1" Grid.Column="1"
                 Text="{Binding Brand, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="{StaticResource ModelLabel}" />
        <TextBox Grid.Row="2" Grid.Column="1"
                 Text="{Binding Model, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}" />

        <TextBlock Grid.Row="3" Grid.Column="0" Text="{StaticResource YearLabel}" />
        <TextBox Grid.Row="3" Grid.Column="1"
                 Text="{Binding Year, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}" />

        <TextBlock Grid.Row="4" Grid.Column="0" Text="{StaticResource PriceLabel}" />
        <TextBox Grid.Row="4" Grid.Column="1"
                 Text="{Binding Price, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}" />

        <TextBlock Grid.Row="5" Grid.Column="0" Text="{StaticResource FuelTypeLabel}" />
        <ComboBox Grid.Row="5" Grid.Column="1"
                  ItemsSource="{Binding FuelTypeOptions}"
                  SelectedItem="{Binding FuelType}" />

        <TextBlock Grid.Row="6" Grid.Column="0" Text="{StaticResource StatusLabel}" />
        <ComboBox Grid.Row="6" Grid.Column="1"
                  ItemsSource="{Binding StatusOptions}"
                  SelectedItem="{Binding Status}" />

        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{StaticResource OkButton}" Command="{Binding SaveCommand}" />
            <Button Content="{StaticResource CancelButton}" Command="{Binding CancelCommand}" />
        </StackPanel>
    </Grid>
</Window>
